# CPP

to run c++ in mac terminal use:-

g++ file_name.cpp -o output_filename
output_filename is the name of the output file set by the user that is created once the program is executed


run the command => ./<output_filename>
to see the output
